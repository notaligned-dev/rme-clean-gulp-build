const sourceFolder = './src/assets';
const buildFolder = './assets';
const isProxy = true; // For dev with other local server
const localWebsiteURL = 'http://gulptest.loc';

export default {
    sourceFolder,
    buildFolder,
    isProxy,
    localWebsiteURL
};