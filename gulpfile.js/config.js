const sourceFolder = './src/assets';
const buildFolder = './assets';
const isProxy = false;
const localWebsiteURL = 'http://localhost';

export default {
    sourceFolder,
    buildFolder,
    isProxy,
    localWebsiteURL
};